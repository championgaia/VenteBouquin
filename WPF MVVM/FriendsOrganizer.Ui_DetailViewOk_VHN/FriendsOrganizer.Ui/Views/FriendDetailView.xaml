<UserControl x:Class="FriendsOrganizer.Ui.Views.FriendDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FriendsOrganizer.Ui.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Margin="2">FirstName</Label>
        <TextBox Grid.Row="1" Text="{Binding CurrentFriend.FirstName}" Margin="5"></TextBox>
        <Label Grid.Row="2" Margin="2">LastName</Label>
        <TextBox Grid.Row="3" Text="{Binding CurrentFriend.LastName}" Margin="5"></TextBox>
        <Label Grid.Row="4" Margin="2">Email</Label>
        <TextBox Grid.Row="5" Text="{Binding CurrentFriend.Email}" Margin="5"></TextBox>
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Center" Margin="20">
            <Button Content="Delete" 
                    Command="{Binding SaveCommande}" 
                    CommandParameter="{Binding RelativeSource={RelativeSource Self}, Path=Content}" 
                    Margin="5" Width="50"></Button>
            <Button Content="Save" 
                    Command="{Binding SaveCommande}" 
                    CommandParameter="{Binding RelativeSource={RelativeSource Self}, Path=Content}" 
                    Margin="5" Height="30" Width="50"></Button>
            <Button Content="New" 
                    Command="{Binding SaveCommande}" 
                    CommandParameter="{Binding RelativeSource={RelativeSource Self}, Path=Content}" 
                    Margin="5" Height="30" Width="50"></Button>
        </StackPanel>
    </Grid>
</UserControl>
